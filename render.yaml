services:
  - type: web
    name: sugarcane-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.webapp.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
