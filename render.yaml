services:
  - type: web
    name: sugarcane-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 120 --workers 1 --threads 1 src.webapp.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
